# Nixpacks configuration for Railway
# Railway uses Nixpacks to build applications

[variables]
NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

