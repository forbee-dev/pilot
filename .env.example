# Railway will set PORT automatically
# No environment variables required for basic setup

# Optional: If you need to reference API URL in client code
# NEXT_PUBLIC_API_URL=https://your-app.railway.app
